---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: aegiswatches.ops.gopherguard.dev
spec:
  group: ops.gopherguard.dev
  names:
    kind: AegisWatch
    listKind: AegisWatchList
    plural: aegiswatches
    singular: aegiswatch
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.targetRef.name
      name: Target
      type: string
    - jsonPath: .status.healingScore
      name: HealingScore
      type: integer
    - jsonPath: .spec.safeMode
      name: SafeMode
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AegisWatch is the Schema for the aegiswatches API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AegisWatchSpec defines the desired state of AegisWatch.
            properties:
              gitRepo:
                description: |-
                  GitRepo is the "owner/repo" string for the GitHub repository where
                  GopherGuard will open pull requests with YAML patches.
                type: string
              gitSecretRef:
                description: |-
                  GitSecretRef names a Kubernetes Secret containing a GitHub token
                  under the key "token".
                type: string
              llmModel:
                default: llama3-70b-8192
                description: |-
                  LLMModel is the model identifier sent to the LLM provider.
                  e.g. "llama3-70b-8192" for Groq, "llama3" for Ollama.
                type: string
              llmProvider:
                default: groq
                description: LLMProvider selects which LLM backend to use for diagnosis.
                enum:
                - groq
                - ollama
                - openai
                type: string
              llmSecretRef:
                description: |-
                  LLMSecretRef names a Kubernetes Secret that contains the LLM API key
                  under the key "apiKey". Not required for Ollama.
                type: string
              restartThreshold:
                default: 3
                description: |-
                  RestartThreshold is the number of container restarts that triggers
                  an anomaly. Defaults to 3.
                format: int32
                minimum: 1
                type: integer
              safeMode:
                default: false
                description: |-
                  SafeMode disables automatic PR creation; GopherGuard will only log
                  the diagnosis and suggested patch.
                type: boolean
              targetRef:
                description: TargetRef points to the Deployment that GopherGuard should
                  watch and heal.
                properties:
                  name:
                    description: Name of the Deployment to watch.
                    type: string
                  namespace:
                    description: Namespace of the Deployment. Defaults to the AegisWatch
                      namespace.
                    type: string
                required:
                - name
                type: object
            required:
            - gitRepo
            - gitSecretRef
            - targetRef
            type: object
          status:
            description: AegisWatchStatus defines the observed state of AegisWatch.
            properties:
              conditions:
                description: Conditions provides standard Kubernetes condition reporting.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              healingScore:
                description: HealingScore counts the number of successful healing
                  PRs created.
                format: int32
                type: integer
              lastAnomalyTime:
                description: LastAnomalyTime records when the most recent anomaly
                  was detected.
                format: date-time
                type: string
              lastDiagnosis:
                description: LastDiagnosis holds the most recent LLM root-cause summary
                  and witty line.
                type: string
              lastPRUrl:
                description: LastPRURL is the GitHub Pull Request URL created by the
                  last healing cycle.
                type: string
              phase:
                description: Phase is the current healing lifecycle phase.
                enum:
                - Watching
                - Degraded
                - Healing
                - Healthy
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
